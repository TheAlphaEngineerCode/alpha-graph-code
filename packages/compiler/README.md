# @alpha-graph/compiler

Pipeline de compilação, capability model em três estados e diagnósticos.

**Estado:** vazio. Recebe implementação na **Fase 5** do
[plano de implementação](../../docs/IMPLEMENTATION_PLAN.md).

A semântica que este pacote deve obedecer é normativa e está em
[`specs/`](../../specs/) — se o código divergir da spec, o código está errado
(invariante 2 do [`AGENTS.md`](../../AGENTS.md)).
