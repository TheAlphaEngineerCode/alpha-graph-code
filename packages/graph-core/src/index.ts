// @alpha-graph/graph-core — Alpha Graph IR: tipos, JSON Schema, parser, validador, normalização, serialização canônica e migrations.
//
// Bootstrap da Fase 0: o pacote existe, tem tooling e entra no build, e ainda não
// tem código. A implementação chega na Fase 2 (ver docs/IMPLEMENTATION_PLAN.md).
// Nada aqui é placeholder de implementação: é um módulo vazio, deliberadamente.

export {};
